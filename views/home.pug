extends layout

block content
  h1 Looking Down
  p.lead Satelite analytics.
  hr
  a.btn.btn-primary(href='/location/add') Create location
  if locations.length > 0
    .row.mt-5
      .col-md-6
        h2 Locations
        for location in locations
          .row
